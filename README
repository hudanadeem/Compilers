Isabella McIvor (1101334)
Huda Nadeem (11439431)
Zuya Abro (1109843)
 
Checkpoint Two
--------------
 
1. Compiling:
make
 
2. Running:

* Run only parser:
  (prints syntax tree to <filename>.abs)
    java -cp /usr/share/java/cup.jar:. CM -a tests/filename.cm

* Run only parser and semantic analyzer:
  (saves syntax tree to <filename>.abs and symbol table to <filename>.sym)
    java -cp /usr/share/java/cup.jar:. CM -s tests/<filename>.cm

  * Note: Semantic analyzer will not run if there are parse errors

* Run parser, semantic analyzer and assembly generator:
  (saves syntax tree to <filename>.abs, symbol table to <filename>.sym and assembly code to <filename.tm>)
    java -cp /usr/share/java/cup.jar:. CM -c tests/<filename>.cm

  Example:
    java -cp /usr/share/java/cup.jar:. CM -c tests/given_tests_C1/fac.cm

3. 

Test folder

*******************************CHECKPOINT 1*******************************
  *Provided test files:
    java -cp /usr/share/java/cup.jar:. CM tests/given_tests_C1/filename.cm
  
  *Our test files:
    java -cp /usr/share/java/cup.jar:. CM tests/12345_C1/filename.cm

*******************************CHECKPOINT 2*******************************
  *Provided test files:
    java -cp /usr/share/java/cup.jar:. CM tests/given_tests_C2/filename.cm
  
  *Our test files:
    java -cp /usr/share/java/cup.jar:. CM tests/filename.cm

*******************************CHECKPOINT 3*******************************

    java -cp /usr/share/java/cup.jar:. CM -c tests/given_tests_C1/fac.cm

